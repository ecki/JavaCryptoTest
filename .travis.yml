language: java

install: mvn -B -X -q clean

# clean / compile - test - package - verify - install - deploy / site
script: mvn verify

jdk:
  - oraclejdk8

notifications:
  email:
    - bernd-2017@eckenfels.net
